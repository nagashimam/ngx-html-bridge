import { Component } from '@angular/core';

@Component({
  selector: 'app-ngif-with-then',
  imports: [],
  templateUrl: './ngif-with-then.html',
  styleUrl: './ngif-with-then.scss'
})
export class NgifWithThen {
  condition: boolean = true;
}
