import { Component } from '@angular/core';

@Component({
  selector: 'app-for',
  imports: [],
  templateUrl: './for.html',
  styleUrl: './for.scss'
})
export class For {
  items: string[] = ['one', 'two', 'three'];
}
